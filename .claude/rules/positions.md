---
paths:
  - "notes/positions/**"
---

When editing position notes:
- Check for an existing Evolution section before appending events
- Never change the `classification:` field without explicit user confirmation
- Preserve `thesis_layer:` — it drives prioritization in the adversarial tribunal
- Evidence For / Evidence Against sections are append-only: add new entries, don't rewrite existing ones
- When updating `stage:`, note the previous stage in the Evolution section
